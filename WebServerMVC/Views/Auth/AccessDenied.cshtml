@{
    ViewData["Title"] = "접근 거부";
    Layout = "_Layout";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card text-center">
                <div class="card-body">
                    <div class="mb-4">
                        <i class="fas fa-lock fa-5x text-danger"></i>
                    </div>
                    <h2 class="card-title text-danger">접근 거부</h2>
                    <p class="card-text">
                        이 페이지에 접근할 권한이 없습니다.<br>
                        관리자 권한이 필요한 페이지입니다.
                    </p>
                    <div class="mt-4">
                        <a asp-controller="Auth" asp-action="Login" class="btn btn-primary me-2">
                            <i class="fas fa-sign-in-alt me-1"></i>로그인
                        </a>
                        <a asp-controller="Home" asp-action="Index" class="btn btn-outline-secondary">
                            <i class="fas fa-home me-1"></i>홈으로
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>